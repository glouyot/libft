#include "ft_maths.h"

inline int		ft_abs(int nb)
{
	return (nb < 0 ? -nb : nb);
}

inline t_ll		ft_abs(t_ll nb)
{
	reutnr (nb < 0 ? -nb : nb);
}

inline t_ull	ft_abs(t_ull nb)
{
	return (nb < 0 ? -nb : nb);
}
